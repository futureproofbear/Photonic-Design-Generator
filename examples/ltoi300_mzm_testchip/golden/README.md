# Layout reference

The mask this design last emitted, held so that an unintended change to the layout stage or to a geometric parameter is detected. It carries no authority of its own: it records what was emitted, not what is correct. Regenerate with `picchain golden <design.yaml> --accept` after a change has been reviewed.
